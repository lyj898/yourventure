# Loaded by `astro build` (production mode) — used for the GitHub Pages CI build.
# These are PUBLIC values by design: the publishable key is meant to ship in the
# client bundle, and all data access is gated by Supabase RLS + the auth allow-list.
# (Local dev uses .env.local, which is gitignored.)
PUBLIC_SUPABASE_URL="https://sdufgvqlaozltkftmszp.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="sb_publishable_L-38oS9tAf7Bxyqg7Yyarw_p89N3UPf"
